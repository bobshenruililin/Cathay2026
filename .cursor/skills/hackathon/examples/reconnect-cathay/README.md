# Example fixture — Reconnect / Cathay (not RoomPulse)

Pattern only: what a **mode output** looks like on this host. Not a product
feature. Not occupancy-vs-booking. Not a 24h Feature-Zero clock — that clock
already elapsed (`docs/DEMO.md`).

Event shape: **corporate bonus-stake × gated-then-sprint**.
Object: missed-connect iPad. Theater: typhoon button.

Use with `.cursor/skills/hackathon/SKILL.md`. After a mode, print a
checkpoint like `checkpoint.md`, then **stop**. Axes for other events:
`references/archetype.md`. Pitch is `apply` + `90s`, not a fixture of its
own.
