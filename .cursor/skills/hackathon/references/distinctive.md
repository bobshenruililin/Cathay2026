# Hallway identity

Judges manage **doubt** and **memory**. They must retell one noun in a
corridor. If they cannot, we are the median loser.

## This host

| Field | Locked line |
| --- | --- |
| Problem | HKG transfer agents recover missed connections in the last 90 minutes (MCT + walk + UM / WCH / unsplittable party). Altéa Q77 stays cryptic. Mass IROPS does not own the desk. |
| Object | One connection on one worker iPad. |
| Hallway | “the desk that doesn’t split the family” |
| Forbidden hallway | “the AI agent dashboard” / “the aviation copilot” / “the super-app” |
| Pairing | Transfer agent + Airport Systems (FlyLab: real role + IT). LLM last. |
| Click | Station clock + live queue → typhoon → CX254 +180 → verbatim `reasoning[]` → guarded draft → Approve. |

**Sameness kill:** if the identity noun is “AI”, “agent”, or “dashboard”,
rewrite the sentence. No code. Beat `median_loser` or pick another angle —
and “another angle” is still this desk, not a new problem.

## Median loser to beat

| Loser | Smell | Response |
| --- | --- | --- |
| ChatGPT trip planner | Generic aviation AI | Named desk. Q77. Engine ranked it. |
| Fight4Flight (2017 RU) | Passenger delay app, no desk | Invert: agent iPad, not passenger `/chat`. |
| U-Explore / Kairos (2025 student) | Lifestyle / loyalty journey | Steal named-BU; do not build U-Explore. |
| FlyVision | Last year’s cargo-CV copy | Do not copy NAAR’s trophy. |
| FlightFixer / AegisOps / SkyResolve-the-orchestrator | Six-agent IROPS factory | Engine `dependencies: {}`. One glob. |
| SMASH (2024) | Pain without object; robot theater | UM / WCH / party chips are the object. Typhoon only loads the bank. |

SQXP and ConnectionSaver are **citations** inside `identity` / `90s`, not
pivots. Hold-the-outbound stays in `docs/LATER.md`.

## Who scores (FlyLab accent)

Corporate bonus-stake: name the **2026 owner** (Airport Systems / HKG
Transfer). Do not lead with a model. Do not let the LLM rank or rebook.
`reasoning[]` on screen is the engine’s `string[]`, verbatim.

## Retell test (`stop` + `identity`)

Ask: after 90 seconds, can a stranger say “the desk that doesn’t split
the family”? If they say “the AI one” or “the typhoon button”, the
checkpoint fails. Rewrite copy. Do not add a feature to compensate.
