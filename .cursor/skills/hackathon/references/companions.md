# Companions (do not bolt on)

`/hackathon` is **one** skill and **one** spine file. A 26-skill kit loses
the freeze. If someone wants to install companions, say no.

## Forbidden companions

| Temptation | Why not | Do this instead |
| --- | --- | --- |
| `/swarm`, `/orchestrate`, best-of-n builders | Feature factory. FlightFixer. | `stop`. One claim after human unfreeze. |
| Champion-named cloud workers with branches | Same factory, funnier names | Voices inside modes. No seat gets a branch. |
| Second skill named `/auto-hackathon` | Split brain. Mix recon into freeze. | Modes of **this** skill. |
| Slide factory / auto-submit / “we will win” | Scoring function has no v2 | Working click + honest LIVE. |
| TDD pipeline as the product | Judges do not score the harness | `pnpm test:demo` stays green. |
| Host MCP that “scores” the pitch | Out of SCOPE | Human + published rubric. |
| Compound during freeze | Different OS; looks like recon-rebuild | `.cursor/skills/compound/` only when asked, never 14–16 Nov as cover. |
| RoomPulse / occupancy-vs-booking example | Wrong sport (24h table-surprise) | `examples/reconnect-cathay/` |
| `HACKATHON.md` replacing `docs/DEMO.md` | Forks the problem | Mirror. Cut, don’t replace. |
| Passenger `/chat`, IFE, loyalty, green points | Median loser / last year’s trophy | Identity rewrite; no code. |
| Kafka, Gurobi, OpenMCT embed, LLM ranking | COMPOUND Never / LATER | Never test or LATER line. Do not build. |
| Seventh DEMO step | Sacred six | Talk beat (SIM / offline draft). |
| Hardware trolley / robot | GingTrip / Niki Jr. theater, not our object | Typhoon button already ships. |

## Allowed reads (not companions)

These already exist on the host. Point at them. Do not wrap them in new
skills.

- Agent contract: `AGENTS.md`
- Demo: `docs/DEMO.md`, `.cursor/rules/demo.mdc`
- Engine purity: `.cursor/rules/engine.mdc`
- Verifier: `.cursor/agents/verifier.md` (after SCOPE code, not a winner seat)
- Claims board: `docs/claims/`
- Compound OS: `.cursor/skills/compound/SKILL.md` (separate sitting)
- 90s words: `docs/competitive/saturday.md`

## Integrate?

**Yes:** one skill, one `docs/HACKATHON.md`, modes that do the next step,
anti-swarm freeze.

**No:** builder swarm, slide factory, TDD-as-product, host MCP, waiting
on a sibling VM’s GitHub remote.
