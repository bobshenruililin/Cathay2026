# HACKATHON.md — spine mirror

Not a second product. Mirrors `docs/DEMO.md` + `docs/SCOPE.md` +
`docs/PITCH_NOTES.md`. Later modes **cut**. They do not replace the problem.
Only a human unfreezes.

| Field | Locked |
| --- | --- |
| Event | Cathay Hackathon 2026 |
| Archetype × clock | Corporate bonus-stake × gated-then-sprint |
| First product | 27 Sep 2026 application |
| Object | One connection on one worker iPad |
| Hallway | “the missed-connect iPad” / “the typhoon-button desk” |
| Click | `docs/DEMO.md` six steps + `docs/competitive/saturday.md` |
| Human freeze | **Locked.** Agents cannot add SCOPE. |

Skill: `.cursor/skills/hackathon/SKILL.md`. Event prior:
`.cursor/skills/hackathon/references/events.md`.

## Prize math (same path)

| Slot | Play for | Artifact |
| --- | --- | --- |
| Primary | 2026 Airport Systems / HKG Transfer owner (FlyLab-shaped) | `docs/PILOT_PROPOSAL.md` + six-step demo |
| Piggyback | Cathay Innovate / intern-path (NAAR-shaped) | The **same** 30-day desk pilot |

No cargo CV, trolley, loyalty app, or second spine “to look ready”.

## LIVE claims

Checkpoint **fails** if a spoken LIVE line has no path. SEEDED = `SIM`.

| LIVE / demo claim | Path |
| --- | --- |
| HKG station clock, not `Date.now()` | `packages/sim` + header; DEMO step 1 |
| SIM badge; offline draft if API/guard dies | station header; talk beat, not step 7 |
| At-risk queue; healthy silent | `triageConnection` / `generateOptions` |
| Handling chips UM / WCH / party | queue + panel; engine SSR map |
| `reasoning[]` verbatim; model did not pick the flight | engine + console render |
| Typhoon +90, queue in place, Peak chip | `sim.injectTyphoon()`; DEMO step 3 |
| CX254 +180; named PNRs | DEMO step 4; `packages/data` pins |
| Recovery option selectable | DEMO step 5 |
| Guarded draft; invented flights → Cathay Alert | `apps/console/lib/llm/guard.ts` + `draft.ts` |
| Approve removes PNR | DEMO step 6 |
| Stage numbers from sim | `demoMetrics("hkg-demo")`; `docs/METRICS.md` |
| Week 1 no Altéa write; beside Passenger Recovery | `docs/PILOT_PROPOSAL.md` |
| Engine `dependencies: {}`; no Gurobi / Kafka / OpenMCT | purity / Never tests |

If it is not in this table, it is not LIVE.
