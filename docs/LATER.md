# Later

Not this demo. Do not build these because a workstream is going well.

- Cargo / ULD Reconnect (same engine, different entities).
- Party-batch API (one call that keeps several PNRs on the same `partyId`).
- Replacing `um` / `wheelchair` flags with SSR-only input.
- Overnight hotel + meal voucher booking.
- Partner seat inventory beyond the synthetic pool.
- LLM in ranking, feasibility, or option generation.
- Goal 3 data-realism pass (`packages/data/SOURCES.md`, extra canned disruptions).
- Goal 4 pitch extras beyond `docs/PILOT_PROPOSAL.md` (ARCHITECTURE.md, METRICS.md).
- Visual QA against Figma overnight.
